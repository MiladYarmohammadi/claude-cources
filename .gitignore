# IDE / editor state
.idea/
*.iml
.vscode/

# macOS
.DS_Store

# Claude Code: personal overrides stay local; shared config (.claude/settings.json,
# skills, agents, hooks, .mcp.json) is committed.
.claude/settings.local.json

# Build artifacts (`make package` output, stray packaged charts)
.packaged/
*.tgz
